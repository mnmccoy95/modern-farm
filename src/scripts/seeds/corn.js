const cornProperties = [
    {type: "corn",
    height: 180,
    output: 6,
    },
    {type: "corn",
    height: 180,
    output: 6,
    }
];

// const cornFix = cornProperties.slice()

export const createCorn = () => {
    return cornProperties.slice()
}